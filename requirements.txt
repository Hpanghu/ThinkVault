# ThinkVault 依赖清单
# 基础安装：pip install -r requirements.txt
# 完整安装（含本地 Embedding + GPU + OCR 等）：pip install -e ".[all]"

# ===================== 核心依赖 =====================
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0
pydantic>=2.0,<3.0
pymupdf>=1.24.0
python-docx>=1.1.0
chromadb>=0.5.0,<0.6.0
rank-bm25>=0.2.2
psutil>=5.9.0
aiofiles>=24.0.0
python-multipart>=0.0.9
httpx>=0.27.0,<1.0
python-dotenv>=1.0.0
watchdog>=4.0
jieba>=0.42
appdirs>=1.4

# ===================== 可选依赖（通过 pyproject.toml 安装）=====================
# 本地 Embedding 模型：pip install -e ".[local-embedding]"
#   sentence-transformers torch
#
# GPU 加速：pip install -e ".[gpu]" 或手动：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# OCR 识别：pip install -e ".[ocr]"
#   rapidocr-onnxruntime>=1.3.0
#
# PPT/Excel/Media：pip install -e ".[ppt,excel,media]"
#   python-pptx openpyxl faster-whisper ffmpeg-python
#
# 开发测试：pip install -e ".[dev]"
#   pytest
