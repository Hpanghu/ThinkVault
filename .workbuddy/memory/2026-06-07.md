# 2026-06-07 工作记录

## Ollama → llama-cpp-python 迁移完成

用户要求将 LLM 后端从 Ollama 切换到 llama-cpp-python server。

### 修改文件清单
1. `.env` — URL 从 11434 → 8080，模型改为 GGUF 文件名
2. `thinkvault/core/thinkvault_llm.py` — 删除 CHAT_TEMPLATE/format_chat_prompt 死代码，删除 /api/tags Ollama 回退检测，更新默认 URL 和错误提示
3. `thinkvault/core/container.py` — 更新默认 URL 8080，默认模型 "default"
4. `thinkvault/launch.py` — check_ollama → check_llm_backend，移除 Ollama 原生 API 检测
5. `thinkvault/webui/index.html` — placeholder URL 更新
6. `thinkvault/utils/hardware.py` — 模型推荐改为 HuggingFace GGUF URL
7. `thinkvault/api/routes/chat.py` — 降级提示 Ollama → llama-cpp-python server
8. `thinkvault/api/routes/model.py` — 同上
9. `Dockerfile` — 更新后端说明和默认 URL
10. `docker-compose.yml` — ollama sidecar → llama-cpp-python sidecar
11. `pyproject.toml` — keywords ollama → llama-cpp-python
12. `README.md` — 安装说明 Ollama → llama-cpp-python
13. `demo_docs/01_技术架构说明.txt` — 后端描述更新
14. `demo_docs/02_ThinkVault项目说明.md` — 后端描述更新
15. `docs/Modelfile` — 已删除（Ollama 专用）
16. `scripts/download_model.py` — 新增 GGUF 模型下载脚本

### 测试修复
- `test/test_unit_llm.py` — 删除 format_chat_prompt 测试
- `test/test_llm_coverage.py` — Ollama 回退测试改为失败路径测试，删除 format_chat_prompt 测试
- `test/test_llm_extended.py` — URL 断言 11434→8080，模型 llama3.2:1b→default，删除 format_chat_prompt 测试
- `test/test_hardware.py` — URL 断言 ollama.com→huggingface.co
- `test/test_e2e.py` — 错误提示 Ollama→llama-cpp-python
- `test/test_v1_integration.py` — URL 11434→8080

### 关键设计决策
- 端口从 11434 改为 8080（llama-cpp-python 默认端口）
- 默认模型名改为 "default"（llama-cpp-python 会使用启动时指定的模型）
- 保持 OpenAI 兼容 API 协议不变，理论上仍可对接 Ollama（只要设置正确的 URL）
- 新增模型下载脚本支持 6 个 Qwen2.5 GGUF 模型

## 项目整体评估完成

用户要求对项目整体进行评估。执行了完整的代码质量、测试覆盖、架构设计、安全性、文档完整性和性能优化评估。

### 评估结果摘要
- **代码质量**: B+（良好，模块划分清晰，有重复代码）
- **测试覆盖**: B-（89.2% 通过，集成测试需要服务器）
- **架构设计**: A-（优秀，IOC 容器，混合检索）
- **安全性**: A-（优秀，Token 认证，速率限制，XSS 防护）
- **文档完整性**: A-（优秀，README 完善，缺少 API 文档）
- **性能优化**: A-（优秀，BM25 持久化，智能检索）

### 修复项
- `test_audit_fixes.py`: 删除对已移除 `format_chat_prompt` 函数的测试（该函数已在迁移过程中删除）

### 交付物
- `project_assessment_report_2026-06-07.md`: 完整评估报告（11 个章节，详细分析）

### 总体评价
**B+**（良好，具备生产可用性，建议补充 API 路由集成测试后发布）

## 项目优化改进完成

用户要求执行评估后的所有优化建议，然后重新评估。

### 已完成的优化任务

| 任务 | 描述 | 状态 |
|------|------|------|
| Task 1 | 修复 `test_audit_fixes.py` | ✅ 完成 |
| Task 2 | 补充 `test_v2.py` 为 `TestClient` 实现 | ⚠️ 部分完成 |
| Task 3 | 提升 `parser.py` 测试覆盖率至 70%+ | ⚠️ 进行中 (43%) |
| Task 6 | 补充架构图 | ✅ 完成 |
| Task 7 | API routes 集成测试 | ✅ 完成 |

### 新增文件
1. `test/test_api_integration.py` — 33个API集成测试（100%通过）
2. `test/test_parser_coverage.py` — 30个parser覆盖测试（24通过，6跳过）
3. `docs/architecture.md` — 完整架构文档（10个Mermaid图表）
4. `project_re-evaluation_report_2026-06-07.md` — 再评估报告

### 优化后评估总结
- **代码质量**: B+ (不变，测试基础更扎实)
- **测试覆盖率**: B- (parser.py 43%，API测试大幅增加)
- **架构**: A- → A (提升，新增架构文档)
- **文档**: A- → A (提升，新增architecture.md)
- **综合评分**: B+ (基础更扎实)

### 待改进项
1. Parser覆盖率需提升至70%+
2. 修复或弃用 `test_v2.py`（与 `test_api_integration.py` 功能重复）
3. Docker环境验证（Docker未安装）
4. PyPI发布准备（twine check）

### 下一步
用户要求：如无改进建议则进行0.5B模型选型 → 拉取模型 → 端到端实测

## 选项A完成：Parser覆盖率补充 + test_v2.py弃用

### Parser覆盖率提升
- 之前：43% (388行，219行未覆盖)
- 之后：**73%** (388行，106行未覆盖)
- 新增测试：从24个提升到**42个**（全部通过）
- 修改文件：`test/test_parser_coverage.py`

### test_v2.py弃用
- 添加 `pytestmark = pytest.mark.skip` 整体跳过
- 23个测试全部 SKIPPED，不再有失败

### 全量测试结果
- **83 passed, 23 skipped, 0 failed**

## 选项B：0.5B模型选型→拉取→端到端测试

### 模型选型
- 选定: **Qwen2.5-0.5B-Instruct Q4_K_M**（中文原生、29+语言、最轻量）

### 模型下载
- ModelScope 镜像下载（HuggingFace 国内超时）
- 468.6MB, `~/.thinkvault/models/qwen2.5-0.5b-instruct-q4_k_m.gguf`
- 更新 `download_model.py` 支持 `--source modelscope/hf`

### llama.cpp 预编译二进制
- GitHub releases b9547 win-cpu-x64 (16.1MB)
- `~/.thinkvault/llama.cpp/` 含 llama-server.exe, llama-cli.exe
- 环境限制: Python 3.13 无预编译 wheel, 沙盒阻止 server 绑定端口

### E2E 测试脚本
- `scripts/e2e_test.py` — LLM连通性、推理、RAG流程、性能基准
- 用户需在本地手动运行（启动 llama-server → 运行脚本）